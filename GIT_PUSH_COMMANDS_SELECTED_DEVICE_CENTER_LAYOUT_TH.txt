cd "D:\IoT Project\dotwatch"

git status -sb

node .\scripts\verify-selected-device-center-layout.mjs

npm --prefix apps/dashboard run build

git diff --check

git add -- `
  apps/dashboard/src/components/MetricConfigPanel.jsx `
  apps/dashboard/src/styles/devices.css `
  scripts/verify-selected-device-center-layout.mjs `
  README_SELECTED_DEVICE_CENTER_LAYOUT_TH.md `
  VALIDATION_SELECTED_DEVICE_CENTER_LAYOUT_TH.md `
  GIT_PUSH_COMMANDS_SELECTED_DEVICE_CENTER_LAYOUT_TH.txt

git diff --cached --check
git diff --cached --stat

git commit -m "style: refine selected device value scroll and security"

git pull --rebase --autostash origin main

git push origin HEAD:main

git fetch origin main
git rev-parse HEAD
git rev-parse origin/main
